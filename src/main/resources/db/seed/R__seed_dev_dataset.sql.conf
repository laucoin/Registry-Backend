# The seed commits in chunks (COMMIT inside DO blocks) because the dataset is huge;
# PostgreSQL only allows transaction control in a DO block when it is not wrapped in
# an outer transaction, so Flyway must run this script without one.
executeInTransaction=false
